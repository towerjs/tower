services:
  postgres:
    image: postgres:17-alpine
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: tower
      POSTGRES_USER: tower
      POSTGRES_PASSWORD: tower
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U tower -d tower']
      interval: 2s
      timeout: 5s
      retries: 15

volumes:
  pgdata:
