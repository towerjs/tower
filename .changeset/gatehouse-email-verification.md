---
'@towerjs/gatehouse': minor
---

Add an `emailVerification` option to the Gatehouse config with a `method` field supporting `link` and `otp` email verification flows, a `required` flag, and a `sendVerificationOTP` callback for OTP delivery.
