{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "@towerjs/with-nextjs#build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**"]
    }
  }
}
